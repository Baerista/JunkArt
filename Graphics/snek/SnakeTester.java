package snek;

import java.util.*;
import java.io.*;
import java.awt.*;
import javax.swing.*;

public class SnakeTester{
    public static void main(String [] cheese){
        /*int [] bounds = {0, 0, 700, 700};

        ArrayList<Snake> theSnakes = new ArrayList<Snake>();


        theSnakes.add(new LineGroup(bounds, angle, length, fade, colors1));
        theGroups.add(new LineGroup(bounds, angle, length, fade, colors2));
        

        SnakeWorld World1 = new LineWorld(bounds, theSnakes); 
        JFrame frame = new JFrame("Line World");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setLocationRelativeTo(null);
        frame.setSize(720, 710);  
        frame.addKeyListener(World1);

        frame.getContentPane().add(World1);

        frame.setVisible(true);
        frame.setLocation(20,10);
        frame.setSize(720, 710);

        World1.run();*/
    }
}